<Window x:Class="InformationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Information" 
    Height="250" 
    Width="350"
    MinHeight="110"
    MinWidth="350"
    ResizeMode="CanResizeWithGrip"        
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner" Topmost="True">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="64" />
      <RowDefinition Height="*" />
      <RowDefinition Height="40" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="64" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Image Grid.Column="0" Grid.Row="0" 
           Margin="16"
           Height="32" Width="32"
           Source="{Binding CurrentIcon}" />
    <Label Grid.Column="1" Grid.Row="0" 
           Height="64"
           Content="{Binding CurrentMessage}" />
    <GroupBox Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Header="Messages"
              Visibility="{Binding LogMessageVisibility}">
      <TextBlock Text="{Binding LogMessageText}" ScrollViewer.CanContentScroll="True" />
    </GroupBox>
    <Button Grid.Column="1" Grid.Row="2" Name="btnOk" 
            Width="100" Margin="8"
            HorizontalAlignment="Right"
            IsDefault="True">OK</Button>
  </Grid>
</Window>
